<div class="container mt-5">
    <h2 class="text-center mb-4">Editar Agendamento</h2>

    <?php if (!empty($msg)): ?>
        <div class="alert alert-warning text-center">
            <?= $msg ?>
        </div>
    <?php endif; ?>

    <form action="" method="post" class="mx-auto" style="max-width: 500px;">
        <div class="mb-3">
            <label for="profissional_id" class="form-label">Profissional</label>
            <select class="form-select" id="profissional_id" name="profissional_id" required>
                <option value="">Selecione</option>
                <?php foreach ($retornoProfissional as $rp): ?>
                    <option value="<?= $rp->id ?? $rp->prof_id ?>"
                        <?= ($agendamento->getProfissional()->getId() == ($rp->id ?? $rp->prof_id)) ? 'selected' : '' ?>>
                        <?= $rp->nome ?? $rp->prof_nome ?>
                    </option>
                <?php endforeach; ?>
            </select>
        </div>

        <div class="mb-3">
            <label for="servico_id" class="form-label">Serviço</label>
            <select class="form-select" id="servico_id" name="servico_id" required>
                <option value="">Selecione</option>
                <?php foreach ($retornoServico as $rs): ?>
                    <option value="<?= $rs->id ?? $rs->serv_id ?>"
                        <?= ($agendamento->getServico()->getId() == ($rs->id ?? $rs->serv_id)) ? 'selected' : '' ?>>
                        <?= $rs->nome ?? $rs->serv_nome ?> - R$ <?= number_format($rs->preco ?? $rs->serv_preco, 2, ',', '.') ?>
                    </option>
                <?php endforeach; ?>
            </select>
        </div>

        <div class="mb-3">
            <label for="data_hora" class="form-label">Data e Hora</label>
            <input type="datetime-local" class="form-control" id="data_hora" name="data_hora"
                value="<?= date('Y-m-d\TH:i', strtotime($agendamento->getDataHora())) ?>" required>
        </div>

        <div class="mb-3">
            <label for="observacoes" class="form-label">Observações</label>
            <textarea class="form-control" id="observacoes" name="observacoes" rows="3"><?= htmlspecialchars($agendamento->getObservacoes()) ?></textarea>
        </div>

        <div class="d-grid">
            <button type="submit" class="btn btn-primary">Salvar Alterações</button>
        </div>

        <?php if (!empty($erro)): ?>
            <div class="alert text-danger text-center fw-bold alert-danger animate__animated animate__shakeX">
                <?= $erro ?>
            </div>
        <?php endif; ?>
    </form>
</div>